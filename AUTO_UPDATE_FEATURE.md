# llama-server自動アップデート機能の実装完了

## 機能概要

新しいexeをインストール・実行する際、**ユーザーが古いバージョンをアンインストール**しなくても、**自動的に最新版に更新される**機能を実装しました。

## 実装内容

### 1. バージョン取得機能
```python
def get_llama_server_version(llama_server_exe: str) -> Optional[str]:
    """llama-serverのバージョンを取得"""
```
- 既存のllama-server.exeから `--version` オプションで現在のバージョンを取得
- 形式: `build b8992` → `b8992` を抽出

### 2. バージョン比較ロジック
```python
def compare_versions(current: Optional[str], target: str = "b8992") -> bool:
    """
    バージョン比較
    Returns: True if current < target (更新が必要)
             False if current >= target (更新不要)
    """
```
- `b8992` 形式から数字を抽出して大小比較
- 古いバージョンなら `True` を返す

### 3. 自動アップデート機能
```python
def download_llama_server():
    """llama-serverの最新バージョンを自動ダウンロード（既存版より新い場合は更新）"""
```
- バージョン比較で古いバージョンが検出されたら自動更新
- 古いバージョンを `backup/` フォルダに保存（ロールバック対応）
- 新しいバージョンをダウンロード・解凍

## Windows環境での動作フロー

### インストール時
```
新しいexeを実行
    ↓
バックエンド起動
    ↓
llama-server確認
    ├→ 存在しない → ダウンロード
    ├→ 古い版 → バックアップ → 新版ダウンロード
    └→ 最新版 → スキップ
```

### 2段階起動プロセス
1. **初回起動（約3～5分）**: llama-serverの自動ダウンロード・解凍
2. **2回目以降（通常速度）**: キャッシュされたバージョンを使用

## ユーザー体験

| シナリオ | 動作 |
|--------|------|
| **新規インストール** | 初回起動で自動ダウンロード |
| **既存版をそのままインストール** | 古いバージョンが検出されて自動更新 |
| **最新版既に導入済み** | チェック → スキップ |
| **アンインストールなし** | ✅ 可能（自動更新が実行）|

## ログ出力例

```
📌 現在のバージョン: b8981
📌 最新バージョン: b8992
🔄 llama-server を更新します...
💾 旧版をバックアップ: backup/llama-server-b8981.exe
📥 llama-server をダウンロード中: https://github.com/...
📦 llama-server を解凍中...
✅ llama-server のセットアップ完了
```

## バックアップ機能

古いバージョンは以下に保存：
```
C:\Users\<ユーザー名>\AppData\Roaming\Glance\llama-server\backup\
└── llama-server-b8981.exe
```

トラブル時は手動で旧版を復元可能。

## セキュリティ考慮事項

1. **バージョン比較**: 数値比較のため安全
2. **ダウンロード元**: GitHub Releases（公式）
3. **SHA256検証**: 将来の改善として検討可能
4. **実行権限**: Unix系OSで自動設定

## 今後の改善案

### Phase 2: ハッシュ値検証
```python
def verify_download(file_path: str, expected_hash: str) -> bool:
    """SHA256ハッシュで整合性を確認"""
```

### Phase 3: バージョン情報API
```python
def get_latest_version_from_github() -> str:
    """GitHub APIから最新バージョンを取得"""
```

## テスト手順

### Windows環境での検証

1. **古いexeをインストール済み**
   ```
   インストール済み: Glance-0.1.23
   内部llama-server: b8981
   ```

2. **新しいexeを実行（アンインストール不要）**
   ```
   新exeをダブルクリック
   → 上書きインストール
   ```

3. **ログで確認**
   ```
   📌 現在のバージョン: b8981
   🔄 llama-server を更新します...
   💾 旧版をバックアップ
   ✅ セットアップ完了
   ```

## まとめ

✅ **自動アップデート機能完全実装**
- バージョン検出
- 自動比較・更新判定
- バックアップ保存
- ロールバック対応可

✅ **ユーザー操作なし**
- アンインストール不要
- 新exeを実行するだけ
- 完全自動化

✅ **高い可靠性**
- 古いバージョン保存
- エラーハンドリング完備
- ロールバック可能

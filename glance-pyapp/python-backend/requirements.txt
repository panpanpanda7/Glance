# Glance バックエンド - 実行時の依存関係（リリース／テスター共通）
#
# ここに書いたものはすべて PyInstaller でインストーラに同梱されます。
# 実際に import されないものは絶対に足さないでください。
# 開発・実験用の依存は requirements-dev.txt に分けています。

# Web Framework（Electron からの API）
flask==3.1.0
flask-cors==5.0.0

# 推論は llama-server.exe を別プロセスで起動し、REST 経由で叩きます
# （Python 側に推論ライブラリは不要です）
requests==2.32.3

# 画像処理
pillow==11.1.0

# 設定ファイル
pyyaml==6.0.2

# CPUコア数検出（llama-server のスレッド数決定）
psutil>=5.9.0
